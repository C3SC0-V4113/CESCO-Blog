{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "cesco-blog",
  "main": "@astrojs/cloudflare/entrypoints/server",
  "compatibility_date": "2026-07-18",
  "compatibility_flags": ["nodejs_compat", "global_fetch_strictly_public"],
  "assets": {
    "binding": "ASSETS",
    "directory": "./dist",
  },
  "observability": {
    "enabled": true,
  },
  "vars": {
    // Local publishing records the exact purge tags without contacting the
    // Cloudflare API. The adapter refuses this mode on non-loopback hosts.
    "CACHE_PURGE_MODE": "local",
    "CLOUDFLARE_ZONE_ID": "",
    // Cloudflare Access identity, verified in the Worker (ADR-0039). Local mode
    // skips verification and is refused on any non-loopback host, so a deploy
    // that keeps it fails closed. Production values come from the deployment
    // config: ACCESS_MODE=cloudflare, ACCESS_TEAM_DOMAIN and ACCESS_AUD.
    "ACCESS_MODE": "local",
    "ACCESS_TEAM_DOMAIN": "",
    "ACCESS_AUD": "",
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "cesco-blog",
      // Local D1 accepts this sentinel. Production deploys use the validated,
      // ignored config generated by `pnpm run config:production`.
      "database_id": "00000000-0000-0000-0000-000000000000",
      "migrations_dir": "drizzle",
    },
  ],
  "r2_buckets": [
    {
      // Editorial media: inline article images, covers, and generated Open
      // Graph cards. See ADR-0024 and ADR-0028.
      "binding": "BUCKET",
      // The production bucket name is supplied to the generated config.
      "bucket_name": "cesco-blog-media",
    },
  ],
  "kv_namespaces": [
    {
      // Astro's Cloudflare adapter provisions sessions through this binding.
      // It is platform plumbing; admin protection remains Cloudflare Access.
      "binding": "SESSION",
      "id": "00000000000000000000000000000000",
    },
  ],
}
